请仅返回 JSON，不要输出任何额外说明。JSON 结构固定如下：

```json
{
  "alarm": "0",
  "result": "未检测到目标",
  "reason": "整体判定依据，简明扼要",
  "targets": [
    {
      "label": "具体的目标描述",
      "confidence": 0.92,
      "bbox2d": [120, 80, 560, 920]
    }
  ]
}
```

要求：
1. `alarm` 只能是 `"0"` 或 `"1"`。
2. `result` 必须是本次图片测试的结论。
3. `reason` 必须是本次图片测试的判定依据。
4. `targets` 用于前端绘框；未命中时必须返回空数组 `[]`。
5. `bbox2d` 使用归一化坐标系，范围为 `[0, 1000]`，格式为 `[x0, y0, x1, y1]`，且必须满足 `x0 < x1`、`y0 < y1`。
6. `label` 必须可直接展示在图片上；`confidence` 使用 `0~1` 的数值。
